"""Tuples are immutable data structure and wee can't change the 
value and we also can't append the tuple, so we use () for tuples
"""

tuples= (1,3,4,5,6,3,4,5,6,7,7,8,8)
print(tuples.count(5))
print(tuples.count(4))
print(tuples.count(7)) 